# Archived workflows

These files preserve upstream build examples and are not GitHub Actions workflows.
The continued branch maintains Fabric and Quilt only. Its active workflow is
`.github/workflows/continued.yml`; the target list is `maintenance/targets.json`.
Do not restore Forge / NeoForge builds or upstream publishing jobs as part of
future version maintenance.
